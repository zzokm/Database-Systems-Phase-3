services:
  backend:
    build:
      context: ./backend
    env_file:
      - ./.env
    ports:
      - "${BACKEND_EXPOSED_PORT:-5000}:${BACKEND_PORT}"

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:5000}
    env_file:
      - ./.env
    ports:
      - "${FRONTEND_EXPOSED_PORT:-3000}:3000"
    depends_on:
      - backend

