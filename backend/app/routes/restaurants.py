"""
Restaurant-related routes will live here.

This file will include:
- Lookup/list endpoints for UI dropdowns (e.g., `GET /api/restaurants`).
- CRUD endpoints required by the course when they target Restaurant data
  (INSERT/UPDATE/DELETE using raw SQL).

All queries must be raw SQL (no ORM) and parameterized.
"""

