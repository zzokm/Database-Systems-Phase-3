"""
Farms-related routes will live here.

This file will include routes such as:
- Lookup/list endpoints for UI dropdowns (e.g., `GET /api/farms`).
- CRUD endpoints required by the course when they target Farm data
  (INSERT/UPDATE/DELETE using raw SQL).

All queries must be raw SQL (no ORM) and parameterized.
"""

